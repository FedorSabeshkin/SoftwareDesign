# Reactive streams

В примере с backpressure, убрал из onNext добавление возможности отправки нового сообщения, а в onSubscribe указал возможность принятия только 2 сообщений, поэтому publisher не вызовет у subscriber onComplete, т.к. третье сообщение не будет передано подписчику. 
